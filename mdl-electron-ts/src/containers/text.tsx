import * as React from 'react';
import DemoText from '../components/demo-text';

export default React.createClass({
    render: function () {
        return (
            <div>
                <DemoText />
            </div>
        )
    }
})

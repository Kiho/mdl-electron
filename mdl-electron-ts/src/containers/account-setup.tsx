import * as React from 'react';
import AccountSetup from '../components/demo-form';

export default React.createClass({
    render: function () {
        return (
            <div>
                <AccountSetup />
            </div>
        )
    }
})
